Arquivo auxiliar funcionamento do projeto:

1 - A classe "Main" testa o léxico.
2 - A classe "MyLexer" é uma herança de Lexer e resolve o problema dos parentêses aninhados/balanceados.
3 - A clase "Main_Parser" testa o sintático.
5 - A gramática se encontra dentro da pasta "src" do projeto com o nome "portugol2.grammar".
6 - O arquivo de teste usado por mim encontra - se na pasta principal do projeto com o nome "teste.txt".
7 - Na hora de rodar no eclipse lembrar de definir qual classe principal usar no projeto e adicionar o
arquivo de teste como argumento (o código não funciona ainda com definição fixa da pasta do .txt teste).
8 - Dentro da pasta principal do projeto também encontra - se um .jar, "execute_promt", para realizar os
teste via prompt de comando. (Atenção ao gerar novo .jar, definir a classe principal, nesse caso já está
com o Main_Parser).

